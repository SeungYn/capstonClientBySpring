import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
const { kakao } = window;

ReactDOM.render(
 
  <App kakao={kakao} />
  
  document.getElementById('root')
);
