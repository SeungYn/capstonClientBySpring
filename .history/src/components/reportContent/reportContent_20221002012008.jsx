import React from 'react';
import styles from './reportContent.moduel.css'
const ReportContent = ({ setSwitch, reportContent }) => {
	return (
		<main className={styels.} ></main>
	)
} ;

export default ReportContent;
