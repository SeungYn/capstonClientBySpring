import React from 'react';
import styles from './partyMakePage.module.css';

const PartyMakePage = (props) => (
  <section className={styles.partyMakeContainer}></section>
);

export default PartyMakePage;
